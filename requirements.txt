# Core runtime deps
dpkt>=1.9.7            # pure-Python pcap parsing (lightweight, cross-platform)
requests>=2.31.0       # HTTP client for application-layer replay
dnspython>=2.3.0       # DNS queries & manipulation

# Optional/alternative HTTP client (async / advanced features)
httpx>=0.24.0

# CLI / UX / helpers
typer[all]>=0.9.0      # easy CLI framework (optional, nice UX)
tqdm>=4.65.0           # progress bars (optional)
python-dateutil>=2.8.2 # robust timestamp parsing/handling

# Optional advanced / packet-level & parsing tools
scapy>=2.5.0           # packet parsing/manipulation (optional; extra setup on Windows)
pyshark>=0.4.5         # wrapper around tshark (requires tshark installed) - optional

# Packaging / dev
pyinstaller>=5.11.0    # build single-file executables for Windows/Linux (dev tool)
black>=24.3.0          # code formatter (dev)
pytest>=8.4.0          # tests (dev)
